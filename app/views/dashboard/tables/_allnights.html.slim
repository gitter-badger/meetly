table.table.table-striped
  thead
    th Nocleg
    th Kobiety
    th Mężczyźni
    th Razem
  tbody
    - current_event.services.in_service_group('Noclegi').order('name asc').each do |service|
      tr
        td= service.description
        td= participants_with_days_and_services.active.women.select { |participant| participant.services.include?(service)  }.count
        td= participants_with_days_and_services.active.men.select { |participant| participant.services.include?(service)  }.count
        td= participants_with_days_and_services.active.select { |participant| participant.services.include?(service)  }.count
